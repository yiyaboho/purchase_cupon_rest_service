local:
  server:
    port: 5001
app:
  api-items:
    endpoint: https://api.mercadolibre.com/items    
management:
   endpoints:
      web:
         exposure:
            include: prometheus,health,info,metric   
